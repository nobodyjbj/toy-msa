server:
  port: 0 # random port

spring:
  application:
    name: user-service
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true # EUREKA ????? ?????? ??? ????? ??? ???? ?? ??
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka # ? ??? user-service ? ?? ???.
  instance:
    instance-id: ${spring.cloud.client.hostname}:${spring.application.instance_id:${random.value}}
